pandas==0.20.3
numpy==1.13.1
gensim==3.4.0
spacy==2.0.11
nltk==3.2.5
pyLDAvis==2.1.1

Java setup (optional): only needed if the user wishes to recreate the labelled topic model
1.6 or higher

Mallet:
unzip the mallet.rar file included in this distribution
create a %MALLET_HOME% environment variable, setting its value to the directory where Mallet resides (e.g. "C:\mallet").